abstract final class AppDurations {
  static const fast = Duration(milliseconds: 200);
  static const normal = Duration(milliseconds: 400);
  static const slow = Duration(milliseconds: 600);
  static const banner = Duration(seconds: 3);
  static const pulse = Duration(milliseconds: 1500);
  static const splash = Duration(milliseconds: 2000);
  static const splashHold = Duration(milliseconds: 1500);
  static const splashTimeout = Duration(seconds: 2);
}
